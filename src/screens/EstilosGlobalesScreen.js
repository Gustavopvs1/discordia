import React from 'react';
import { StatusBar } from 'expo-status-bar';
import {StyleSheet ,Platform, SafeAreaView, Text, View, ScrollView, Button } from 'react-native';


const EstilosGlobalesScreen = (props) => {
    return(
       <View style={{flex:1, justifyContent: 'center', alignItems: 'center'}}>
           <Text>
               EstilosGlobalesScreen
           </Text>
       </View> 
    );
}

export default EstilosGlobalesScreen;